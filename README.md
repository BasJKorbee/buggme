# buggme

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

To build:

```bash
bun build index.ts
```

To compile:

```bash
bun build index.ts --compile --outfile=BuggMe.exe
```
